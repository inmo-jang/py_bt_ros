scenario:
  environment: scenarios.example_simple

agent:
  namespaces: "/Fire_UGV_1"  # used as ROS namespace for the agent's nodes and topics
  behavior_tree_xml: "default_bt.xml"

bt_runner:
  bt_tick_rate: 10.0
  bt_visualiser:
    enabled: True
    screen_width: 600
    screen_height: 600
  profiling_mode: False
